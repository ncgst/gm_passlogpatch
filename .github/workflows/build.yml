name: build

on:
  push:
    tags:
      - '*.*.*'

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build_linux:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install Linux build dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y gcc-multilib libc6-dev-i386

    - name: Install stable toolchain
      run: |
        rustup toolchain install stable --profile minimal
        rustup default stable

    - name: Add Linux targets
      run: |
        rustup target add i686-unknown-linux-gnu
        rustup target add x86_64-unknown-linux-gnu

    - name: Build Linux 32-bit
      run: cargo build --release --target i686-unknown-linux-gnu

    - name: Build Linux 64-bit
      run: cargo build --release --target x86_64-unknown-linux-gnu

    - name: Strip debug symbols
      run: |
        strip target/i686-unknown-linux-gnu/release/libgm_passlogpatch.so
        strip target/x86_64-unknown-linux-gnu/release/libgm_passlogpatch.so

    - name: Rename Linux binaries
      run: |
        mkdir -p dist
        cp target/i686-unknown-linux-gnu/release/libgm_passlogpatch.so dist/gmsv_passlogpatch_linux.dll
        cp target/x86_64-unknown-linux-gnu/release/libgm_passlogpatch.so dist/gmsv_passlogpatch_linux64.dll

    - name: Upload Linux artifacts
      uses: actions/upload-artifact@v4
      with:
        name: linux-binaries
        path: |
          dist/gmsv_passlogpatch_linux.dll
          dist/gmsv_passlogpatch_linux64.dll

  release:
    runs-on: ubuntu-latest
    needs:
      - build_linux

    steps:
    - uses: actions/checkout@v4

    - name: Download build artifacts
      uses: actions/download-artifact@v4
      with:
        pattern: '*-binaries'
        path: dist
        merge-multiple: true

    - name: Show release files
      run: ls -la dist

    - name: Create draft release with all assets
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        gh release create "${GITHUB_REF_NAME}" dist/* \
          --title "${GITHUB_REF_NAME}" \
          --draft \
          --verify-tag

    - name: Publish release
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        gh release edit "${GITHUB_REF_NAME}" --draft=false
