[package]
name = "gm_passlogpatch"
authors = ["MODcraft"]
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[profile.release]
codegen-units = 1
lto = "thin"
opt-level = "s"
panic = "abort"

[dependencies]
gmod = { version = "11.1.1", default-features = false }
libc = "0.2"